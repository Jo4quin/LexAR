# Datos: paquetes pesados (cientos de MB), no se versionan.
# Se comparten por fuera de git; ver README.md para la ubicacion esperada.
/data/

# Salidas generadas por notebooks/scripts.
/outputs/

# Jupyter
.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Entorno
.env

# Material de estudio personal (no es parte del entregable del proyecto).
/docspropios/

# Paquete temporal para compartir outputs/ por fuera de git (ver README.md).
/outputs.zip

# Documentacion interna: se mantiene local, no se publica en el repo.
/PLAN.md
/CLAUDE.md
